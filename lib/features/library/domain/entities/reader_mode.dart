/// Supported presentation modes for the chapter reader.
enum ReaderMode {
  /// Scroll-based reader where chapter pages are stacked vertically.
  vertical,

  /// Page-by-page reader where each page is shown in a paged viewport.
  paged,
}
